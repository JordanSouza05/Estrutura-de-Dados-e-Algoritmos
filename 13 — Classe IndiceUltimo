public class IndiceUltimo {

    public static int indiceUltimo(String[] vetor, String elemento) {

        for (int i = vetor.length - 1; i >= 0; i--) {

            if (vetor[i] != null && vetor[i].equals(elemento)) {
                return i;
            }

        }

        return -1;
    }

}
